int main(int argc, char *argv[])
{
    /* code */

    // ask for diana state

    // if state:INIT
    
    // create srv for init state

    // if state:RUN

    // pub vel

    // if state:TERM

    // shutdown ros

    return 0;
}
